server:
  port: 2025

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://34.121.186.196:3306/product_db
    username: root
    password: giangnt_userdbD\ppv2/t/NEqdVlJ
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  kafka:
    bootstrap-servers: kafka:9092
    schema-registry:
      url: http://schema-registry:8081

kafka:
  consumer:
    product-group: product-group

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/